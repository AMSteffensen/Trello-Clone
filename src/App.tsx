import { FC } from 'react';
import { Column } from './column';
import { Card } from './card';
import { AppContainer } from './styles';

export const App = () => {
  return (
  <AppContainer>
    Hello
  </AppContainer>
  );
}